# 🗓️ 내일(12.20) B-RAG 작업 계획

## 🎯 핵심 목표
**균형잡힌 Yes/No 분배 달성** (현재 10:0 → 목표 4-6개씩)

## 📋 작업 우선순위

### 🔥 우선순위 1: 프롬프트 균형 분배 강제 (2-3시간)
**현재 문제**: 모든 질문이 Yes 답변으로 편향
**해결 방안**:
1. **강제 분배 메커니즘 추가**
   ```python
   # 프롬프트에 추가할 내용
   "반드시 Yes 답변 5개, No 답변 5개를 생성하세요. 
   이 비율을 지키지 않으면 실패로 간주됩니다."
   ```

2. **구체적 예시 추가**
   - 균형잡힌 분배 샘플 제공
   - Yes/No 각각의 질문 템플릿 명시

3. **검증 강화**
   - 생성 후 즉시 분배 비율 확인
   - 불균형 시 재생성 요청

### 🔥 우선순위 2: Fallback 로직 완전 재설계 (1-2시간)
**현재 문제**: Fallback에서도 모든 질문이 Yes
**해결 방안**:
1. **명시적 분배 로직**
   ```python
   # 레벨 1,3,5,7,9 → Yes
   # 레벨 2,4,6,8,10 → No
   # 하드코딩으로 확실히 보장
   ```

2. **질문 템플릿 다양화**
   - Yes 유도 질문 5개 템플릿
   - No 유도 질문 5개 템플릿

### 🟡 우선순위 3: 질문 다양성 개선 (1-2시간)
**목표**: 레벨별 차별화된 질문 생성
**작업**:
1. 레벨별 프롬프트 세분화
2. 법률 전문성 강화
3. 다양한 관점 반영

## 🧪 테스트 계획

### 1. 단계별 테스트
```python
# 1단계: 기본 분배 테스트
result = generator.generate_ten_level_questions(...)
yes_count = sum(1 for q in result.questions if q.expected_answer.value == 'Yes')
assert 3 <= yes_count <= 7, f"분배 실패: Yes {yes_count}개"

# 2단계: 다양성 테스트  
questions_text = [q.question for q in result.questions]
assert len(set(questions_text)) >= 8, "질문 다양성 부족"

# 3단계: 레벨별 차별화 테스트
level_1_question = result.questions[0].question
level_10_question = result.questions[9].question
assert level_1_question != level_10_question, "레벨별 차별화 실패"
```

### 2. 성능 지표 목표
| 지표 | 현재 | 내일 목표 | 최종 목표 |
|------|------|-----------|-----------|
| 균형도 | 0.00 | 0.4+ | 0.6+ |
| Yes 개수 | 10 | 4-6 | 4-6 |
| No 개수 | 0 | 4-6 | 4-6 |
| 질문 다양성 | 1 | 2 | 2 |
| 평균 확신도 | 0.50 | 0.60+ | 0.70+ |

## 🔧 구체적 수정 포인트

### 1. 프롬프트 수정 (`_create_enhanced_prompt`)
```python
# 추가할 강제 분배 지시문
system_prompt += """
🚨 필수 요구사항:
- 정확히 Yes 답변 5개, No 답변 5개를 생성하세요
- 분배 비율을 지키지 않으면 실패로 간주됩니다
- 각 답변 유형별로 다양한 관점을 포함하세요

예시 분배:
Level 1,3,5,7,9: Yes 답변 (긍정적 관점)
Level 2,4,6,8,10: No 답변 (부정적 관점)
"""
```

### 2. 검증 로직 수정 (`_validate_result`)
```python
# 더 엄격한 균형 검증
if not (3 <= yes_count <= 7 and 3 <= no_count <= 7):
    print(f"❌ 분배 기준 미달: Yes {yes_count}개, No {no_count}개")
    return False
```

### 3. Fallback 로직 수정 (`_create_fallback_questions`)
```python
# 명시적 분배 보장
yes_levels = [1, 3, 5, 7, 9]
no_levels = [2, 4, 6, 8, 10]

for level in range(1, 11):
    if level in yes_levels:
        expected_answer = YesNoAnswer.YES
        # Yes 유도 질문 템플릿
    else:
        expected_answer = YesNoAnswer.NO  
        # No 유도 질문 템플릿
```

## 📊 진행 상황 체크포인트

### 오전 (10:00-12:00)
- [ ] 프롬프트 강제 분배 메커니즘 추가
- [ ] 기본 테스트 실행 및 결과 확인
- [ ] 분배 비율 개선 확인

### 오후 (14:00-17:00)  
- [ ] Fallback 로직 재설계
- [ ] 질문 다양성 개선
- [ ] 종합 테스트 및 성능 지표 측정

### 마무리 (17:00-18:00)
- [ ] 팀원 리뷰 및 피드백
- [ ] 다음 단계 계획 수립
- [ ] 문서 업데이트

## 🚨 예상 이슈 및 대응

### 이슈 1: 프롬프트 수정 후에도 편향 지속
**대응**: 더 강력한 강제 메커니즘 적용
- 단계별 검증 추가
- 예시 더 구체화

### 이슈 2: AI 생성 성공률 저하
**대응**: 검증 기준 완화
- 균형도 기준 조정 (0.4 → 0.3)
- 재시도 횟수 증가

### 이슈 3: 질문 품질 저하
**대응**: 품질과 균형의 절충점 찾기
- 핵심은 균형 분배 달성
- 품질은 2차 목표로 설정

## 📞 협업 계획

### 팀원 역할 분담
- **프롬프트 개선**: [담당자 A]
- **Fallback 로직**: [담당자 B]  
- **테스트 및 검증**: [담당자 C]

### 소통 계획
- **오전 10시**: 킥오프 미팅
- **오후 2시**: 중간 점검
- **오후 5시**: 결과 리뷰

---
**작성일**: 2024.12.19
**목표 완료일**: 2024.12.20 18:00 